# Core LiveKit agent framework
livekit>=0.17.0
livekit-agents>=0.11.0
livekit-plugins-deepgram>=0.6.0
livekit-plugins-elevenlabs>=0.6.0
livekit-plugins-openai>=0.6.0
livekit-plugins-silero>=0.6.0

# LLM
openai>=1.30.0

# STT
deepgram-sdk>=3.5.0

# TTS
elevenlabs>=1.5.0

# Language detection
langdetect>=1.0.9

# Config & env
python-dotenv>=1.0.0
pyyaml>=6.0.1
pydantic>=2.7.0

# Async HTTP (for Sarvam AI)
aiohttp>=3.9.0

# Logging
structlog>=24.1.0

# Backend (optional REST endpoints / health checks)
fastapi>=0.111.0
uvicorn>=0.30.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
